# utils
Repository to host utility scripts for dev setup and others.

To configure the dev environment, follow these steps: 

```
git clone git@github.com:alexovidiupopa/utils.git

cd utils

./bootstrap.sh
```